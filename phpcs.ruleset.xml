<?xml version="1.0"?>
<ruleset>
  <file>.</file>

  <arg name="extensions" value="php"/>

  <exclude-pattern>*/vendor/*</exclude-pattern>

  <arg value="-colors"/>

  <!-- Show sniff codes in all reports -->
  <arg value="s"/>
  <!-- Exclude Warnings -->
  <arg value="n"/>

  <rule ref="PSR2"/>
</ruleset>
